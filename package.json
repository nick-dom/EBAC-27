{
  "name": "diario-de-bordo",
  "version": "1.0.0",
  "private": true,
  "description": "PWA de registro de atividades diárias — HTML, CSS e JS puros, com manifest, service worker e persistência em localStorage.",
  "scripts": {
    "start": "npx serve .",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "eslint": "^9.9.0",
    "html-validate": "^11.16.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "prettier": "^3.3.3"
  },
  "jest": {
    "testEnvironment": "jsdom"
  }
}
